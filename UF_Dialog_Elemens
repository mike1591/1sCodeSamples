
&НаКлиенте
Процедура КаталогНачалоВыбора(ДопПараметры)
	СтандартнаяОбработка = Ложь ;
	Д = Новый ДиалогВыбораФайла (режимдиалогавыборафайла.ВыборКаталога);
	Д.Заголовок = ДопПараметры.Заголовок ;
	
	Оповещение = Новый ОписаниеОповещения("КаталогНачалоВыбораЗавершение",ЭтаФорма,ДопПараметры) ;
	
	Д.Показать(Оповещение);
	
КонецПроцедуры

&НаКлиенте 
ПРоцедура КаталогНачалоВыбораЗавершение(ВыбранныеФайлы,ДопПараметры) Экспорт 
	Если  ВыбранныеФайлы <> Неопределено Тогда 
		Если ДопПараметры.Свойство("Колонка") Тогда 
			ЭтотОбъект.ТекущийЭлемент.ТекущиеДанные[ДопПараметры.Колонка] =ВыбранныеФайлы[0] ;
			Модифицированность = Истина ;
		ИначеЕсли ДопПараметры.Свойство("Реквизит") Тогда 
			Объект[ДопПараметры.Реквизит] =ВыбранныеФайлы[0] ;
			Модифицированность = Истина ;
		КонецЕсли ;
		
	КонецЕсли ;
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузкаЭкспортНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь ;
	ДопПараметры =  Новый Структура ;
	ДопПараметры.Вставить("Заголовок","Выберите каталог экспорта");
	ДопПараметры.Вставить("Колонка","Экспорт");
	КаталогНачалоВыбора(ДопПараметры) ;
КонецПроцедуры
